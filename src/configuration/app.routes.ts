import { Routes } from '@angular/router';
import ProfileView from "../modules/configuration/views/profile/profile.view";

export const routes: Routes = [
    {path: "config/profile", component: ProfileView}
];